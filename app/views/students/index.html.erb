<h1>Students</h1>

<ul>
  <% @students.each do |student| %>
      <li>
        <%= link_to student.title, school_class %>
      </li>
  <% end %>
</ul>

<%= link_to "New School Class", new_school_class_path %>
